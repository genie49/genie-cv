[build]
builder = "RAILPACK"
buildCommand = "bun install --frozen-lockfile"
watchPatterns = ["apps/server/**", "packages/shared/**"]

[deploy]
startCommand = "bun apps/server/src/index.ts"
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
